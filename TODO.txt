1.  lib中进行data_type管理，现在各个函数的返回值指示乱七八糟，全是各种字典 optional的嵌套，很难看

2.  在未来 expected_next_type这个属性应该进行一定的重构，现在这种状态属于纯手动维护，必然会为未来开发带来困难。
    更主要的一点在于，对于不同的target lang来说，语法行为应该有不同的限制。比如python允许函数和类的嵌套定义，但C中不会进行函数的嵌套
    目前的困难是：mccp不是编程语言，目前看下来也不太适合去编写“上下文无关文法 - 也即CFG”。我需要自定义递归下降的结构，保持灵活性和可变性

3.  expected_next_type 这个属性不仅和当前缩进等级关联，还和此前一个缩进等级的行关联。那么其实最好的办法是，只写互相归属的expected，尽量不要写同级的？
    可以尝试利用'is_block_start'这一个属性。这个属性会涉及块起始和缩进级别递增。
    目前想通了，expected和上一级的expected child以及同级上一行的expected sibling有关系，也可能做成只和上一行的expected child有关，我先试试