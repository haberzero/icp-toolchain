# 依赖关系再提炼需求

请你根据[所有文件的最新需求描述]，重新分析并再提炼工程的依赖关系。

**所有文件的最新需求描述：**

以下是所有文件的工作逻辑描述

ALL_FILE_REQUIREMENTS_PLACEHOLDER

**当前的工程结构（JSON格式）：**

```json
CURRENT_JSON_STRUCTURE_PLACEHOLDER
```

**完整路径列表：**

```text
FILE_PATHS_PLACEHOLDER
```

请你综合考虑：

1. 文件级实现规划中描述的整体架构和调用关系
2. 每个文件自身的工作逻辑描述

重新生成精确的依赖关系模型，确保：

- 所有依赖路径必须在工程结构中真实存在
- dependent_relation 中每个文件都有对应条目（即使依赖为空列表）
- 依赖关系能够准确反映文件间的实际调用关系，只要有直接调用就必然有依赖关系

**重要提示**：

- 基础层文件（被其他文件依赖）的 dependent_relation 应该为空列表 []
- 业务层和应用层文件应该依赖基础层文件
- 依赖方向从上层指向下层，确保单向依赖，避免循环依赖
- 所有依赖路径必须在工程文件夹结构中真实存在，你可以参考上面的完整路径列表

请严格按照系统提示词要求的JSON格式输出结果。
