# 目标代码生成

请你将以下经过符号规范化的自然语言代码转换为目标编程语言的完整可执行代码。

## 目标编程语言

TARGET_LANGUAGE_PLACEHOLDER

## 文件路径

CURRENT_FILE_PATH_PLACEHOLDER

## 重要参数

以下是从用户原始需求中提取出的重要参数，请你在完成目标代码时进行参考

EXTRACTED_PARAM_PLACEHOLDER

## 可调用的库文件

以下是你可以使用的库以及其用途，请确保正确导入和使用。不允许使用未列出的库。

LIBRARY_PLACEHOLDER

## 工程目录结构

以下是你需要参考的工程目录结构，描述了当前项目中各个文件和文件夹的组织方式：

PROJROOT_DIRCONTENT_PLACEHOLDER

## 文件级实现规划

以下是整个程序的实现规划，描述了文件调用关系和功能实现路径：

IMPLEMENTATION_PLAN_PLACEHOLDER

## 规范化后的IBC代码

以下是一段名为intent_behavior_code的自然语言代码，这是你需要转化为目标编程语言代码的所有内容。

**重要说明**：

- IBC代码中通过 `class`、`func`、`var` 定义的符号名称已经是规范化的英文标识符，必须直接使用这些名称
- behavior部分的中文描述仅用于理解业务逻辑，生成代码时需要使用英文标识符
- 所有代码标识符（变量名、函数名、类名）必须使用英文，绝对禁止使用中文、日文或拼音

```intent_behavior_code
IBC_CONTENT_PLACEHOLDER
```

## 当前文件符号定义说明

以下是当前文件中定义的符号的使用说明，这些符号是你需要实现的：

LOCAL_SYMBOLS_USAGE_GUIDE_PLACEHOLDER

## 依赖符号使用说明

以下是当前文件引用的外部符号的使用说明，这些符号可以直接调用：

DEPENDENCY_SYMBOLS_USAGE_GUIDE_PLACEHOLDER

## 任务要求

请基于上述IBC代码，生成完整的、可直接运行的目标语言代码文件。不允许遗漏功能，必须完整转换。

*注意事项：**

1. **符号引用**：自然语言代码中的`$符号名`表示外部符号引用，这些符号的详细使用方法已在"依赖符号使用说明"部分说明，请正确导入和使用
2. **结构完整**：生成的代码应包含所有必要的导入语句、类型声明等
3. **功能正确**：准确实现IBC中描述的所有功能和行为逻辑，参考"当前文件符号定义说明"中每个符号的参数要求和功能描述
4. **代码质量**：遵循目标语言的编码规范，代码清晰易读
5. **文档注释**：将description转换为适当的文档字符串或注释
6. **英文标识符（强制）**：所有变量、函数、类的名称必须使用英文，禁止使用中文或其他非英文字符作为标识符。IBC中已定义的符号名称（如类名、函数名、参数名）已经是规范化的英文标识符，必须直接使用，不要重新翻译
7. **参数使用**：严格按照符号定义说明中的参数列表实现函数和类的构造函数，参数名称、顺序和含义必须与说明一致

**输出格式：**

- 仅输出目标语言的源代码
- 不要添加任何解释性文字
- 直接输出可执行的代码文件内容
